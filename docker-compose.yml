version: "3.9"

services:
  winjob:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "<your_port>:8501"
    environment:
      # Tell your app where to reach Ollama
      OLLAMA_HOST: https://ollama.blucera.com
    restart: unless-stopped
